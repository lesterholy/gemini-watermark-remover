# 复制为 .env 后再按需修改：
#   cp env_eaxmple .env
#
# Dokploy / docker compose 可用的环境变量

# 镜像地址（推荐使用 GHCR）
# 例：ghcr.io/<owner>/<repo>:latest
IMAGE=ghcr.io/<owner>/<repo>:latest

# 对外暴露端口（宿主机端口 -> 容器 80）
APP_PORT=8080

